// Exporting all variables that should be constants. Using Upper case since this is a convention for constatnts that will never change.
export const API_URL = "https://forkify-api.herokuapp.com/api/v2/recipes/";
// Exporting the timeout seconds as a variable so our code is more readable. To avoid the "magic number" where we do not know why or where a value is coming from
export const TIMEOUT_SEC = 10;
export const RES_PER_PAGE = 10;
// Store the generated API dev Key into a constant variable and export it
export const KEY = "8b05f614-1aaa-4f4e-ab11-c9edd4103e05";
// Exporting timeout for closing the modal window
export const MODAL_CLOSE_SEC = 2.5;
